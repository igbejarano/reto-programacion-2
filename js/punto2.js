const nota1 = parseInt(prompt("Ingrese un numero"));
const nota2 = parseInt(prompt("Ingrese un numero"));
const nota3 = parseInt(prompt("Ingrese un numero"));

if ((nota1, nota2, nota3 <= 7)) {
  alert(`Promocionado`);
}
